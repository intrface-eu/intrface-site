# Image mode

Convert approved concept directions into GPT Image 2 static concept prompt packs.

Read `brand-strategy.md` and `concept-directions.md`. Produce prompt IDs, exact prompts, brand tokens, format targets, planned output paths under `hyperframes/assets/generated/concepts/`, and negative constraints derived from off-brand rules. Stop at prompt packs and operator approval; do not assume image generation has happened.
