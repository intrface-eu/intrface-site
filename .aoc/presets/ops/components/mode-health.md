# Health mode

Use for diagnostics, runtime health, install repair, AOC checks, and broken shortcut/session behavior.

Start with metadata/status commands. Escalate only when status output identifies a path or component.
