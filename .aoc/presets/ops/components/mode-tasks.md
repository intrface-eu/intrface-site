# Tasks mode

Use for Taskmaster tags, specs, task decomposition, dependency cleanup, and handoff hygiene.

Use `tm`/`aoc-task` commands rather than editing task JSON directly. Keep specs linked and decisions recorded in memory.
