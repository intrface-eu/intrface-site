# Repo mode

Use for codebase evidence, architecture discovery, symbol tracing, and implementation-grounded answers.

Use path-scoped search first. Escalate to RLM only when local grep/read is insufficient.
