# Verify mode

Default mode for local verification after a change.

Checklist:
- identify changed surfaces and expected behavior
- run targeted static checks or unit/integration tests when available
- open the relevant app/route when UI behavior is involved
- inspect console/network/runtime errors
- confirm loading, empty, error, focus, hover, and responsive basics when relevant
- finish with pass/fail evidence and remaining risk
