/**
 * Same card as the Open Graph image. Declared as its own file so `twitter:image`
 * is emitted explicitly rather than inherited, and so the two never drift.
 */
export { default, alt, contentType, size } from "./opengraph-image";
